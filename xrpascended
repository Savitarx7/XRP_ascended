<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>XRP Ascended</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            font-family: 'Arial', sans-serif;
            text-align: center;
            color: white;
        }

        /* Video Background */
        .video-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: -1;
        }

        .video-container video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Main Content */
        .content {
            position: relative;
            z-index: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .title {
            font-size: 5rem;
            font-weight: bold;
            text-transform: uppercase;
            text-shadow: 0 0 20px rgba(255, 215, 0, 0.8);
        }

        .ascend-button {
            background: linear-gradient(45deg, gold, white);
            border: none;
            padding: 15px 40px;
            font-size: 1.5rem;
            font-weight: bold;
            margin-top: 20px;
            cursor: pointer;
            border-radius: 10px;
            box-shadow: 0 0 20px gold;
            transition: transform 0.2s ease-in-out, box-shadow 0.3s;
        }

        .ascend-button:hover {
            transform: scale(1.1);
            box-shadow: 0 0 30px gold;
        }

        .ascended-text {
            display: none;
            margin-top: 30px;
            font-size: 2rem;
            text-shadow: 0 0 15px rgba(255, 215, 0, 0.8);
            animation: fadeIn 2s ease-in-out forwards;
        }

        /* Fade-in Animation */
        @keyframes fadeIn {
            0% { opacity: 0; transform: translateY(20px); }
            100% { opacity: 1; transform: translateY(0); }
        }

        /* Telegram Link */
        .telegram-link {
            margin-top: 20px;
            font-size: 1.2rem;
            text-decoration: none;
            color: #0088cc; /* Telegram blue */
            font-weight: bold;
            text-shadow: 0 0 10px rgba(0, 136, 204, 0.8); /* Blue glow */
        }

        .telegram-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <div class="video-container">
        <video autoplay muted loop>
            <source src="spaace.mp4.mp4" type="video/mp4">
        </video>
    </div>

    <div class="content">
        <div class="title">XRP Ascended</div>
        <button class="ascend-button" onclick="ascend()">Ascend</button>
        <div class="ascended-text" id="ascendedText">
            You have ascended beyond mortal understanding.  
            The cosmic tides of XRP flow through you,  
            and the divine ledger recognizes your presence.  
            Welcome to the celestial realm.
        </div>
        <a href="https://t.me/xrpascended" class="telegram-link" target="_blank">Join our Telegram Community</a>
    </div>

    <script>
        function ascend() {
            document.getElementById("ascendedText").style.display = "block";
        }
    </script>

</body>
</html>
